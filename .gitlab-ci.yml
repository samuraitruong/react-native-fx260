stages:
  - build
  - package
  - deploy
  
before_script:
   

android-build:
  stage: build
  script: 
    - "sudo npm install"
    - "sudo npm run android:build"
  artifacts:
    paths:
      - android/app/build/outputs/apks/*.zip