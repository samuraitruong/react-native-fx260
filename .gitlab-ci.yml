stages:
  - build
  - package
  - deploy
  
before_script:

android-build:
  stage: build
  script: "cd android && .\gradlew.bat assembleRelease"
  artifacts:
    paths:
      - android/app/build/outputs/apks/*.zip